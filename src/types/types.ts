// src/types.ts
export type NavItem = {
    label: string;
    path: string;
};

export type Brand = {
    name: string;
    category: string;
    image_path: string;
    additional_info: string;
    tab_category: string;
};